# AI Automation Agency

- **Business Name:** AI Automation Agency
- **Version:** 0.0.0
- **Status:** template
- **Category:** technology-services (see `../../taxonomy/categories.json` — renamed from `ai-automation-agencies` on 2026-07-10, same business, no folder move)
- **Business Model:** agency (see `../../taxonomy/business-models.json`)
- **Created At:** 2026-07-09
- **Last Updated:** 2026-07-09
- **Canonical:** true — this is **the** reference structure for the new
  Business Library format (`business-library/technology/`). Every future
  business package should copy this folder's shape, the same way
  `examples/ai-automation-agency.ts` served that role for the now-legacy
  Business Genome format.

## Description

Placeholder — to be authored by the product team.

## What's in this package

| File | Mirrors |
|---|---|
| `metadata.json` | Package-level identity/status (new to this format — no legacy equivalent) |
| `business-dna.json` | `features/business-dna`'s `BusinessDnaProfile` (all 21 sections) — see that feature's schema, not `business-library/schema.ts` |
| `blueprint.md` | `BusinessBlueprintTemplate` (`src/features/business-engine/schemas/templates.ts`) |
| `financial.json` | `BusinessFinancialTemplate` (same file) |
| `marketing.json` | `BusinessMarketingTemplate` (same file) |
| `roadmap.json` | `BusinessLaunchTemplate` (same file) |
| `resources.json` | This package's own resource list (see `../../taxonomy/resources.json` for the category vocabulary) |
| `ai-notes.md` | `features/business-dna`'s `AiMetadata` (`types/sections/ai-metadata.ts`) |
| `assets/` | Empty — future images/media for this business |

Every file in this package is a placeholder — no real business content
has been authored yet. See `../../README.md` for the full authoring
walkthrough.
