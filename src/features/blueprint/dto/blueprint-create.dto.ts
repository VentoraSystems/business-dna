import type { Blueprint } from "../types/blueprint";

/** Input to create a new Blueprint — authored whole, all 21 sections required, same pattern as features/business-dna's CreateDto. */
export type BlueprintCreateDto = Blueprint;
