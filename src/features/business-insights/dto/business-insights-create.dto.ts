import type { BusinessInsights } from "../types/business-insights";

/** Input to create a new Business Insights document — authored whole, same pattern as the other Business Assets CreateDtos. */
export type BusinessInsightsCreateDto = BusinessInsights;
