import type { BusinessInsights } from "../types/business-insights";

/** The Business Insights shape as returned to callers — identical to `BusinessInsights` (types/business-insights.ts). */
export type BusinessInsightsDto = BusinessInsights;
