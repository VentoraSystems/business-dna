import type { Marketing } from "../types/marketing";

/** Input to create a new Marketing document — authored whole, all 18 sections required, same pattern as features/blueprint's CreateDto. */
export type MarketingCreateDto = Marketing;
