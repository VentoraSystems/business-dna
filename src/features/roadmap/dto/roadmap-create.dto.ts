import type { Roadmap } from "../types/roadmap";

/** Input to create a new Roadmap — authored whole, same pattern as features/blueprint's, features/financial's, and features/marketing's CreateDto. */
export type RoadmapCreateDto = Roadmap;
