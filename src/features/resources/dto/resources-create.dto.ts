import type { Resources } from "../types/resources";

/** Input to create a new Resources document — authored whole, same pattern as the other Business Assets CreateDtos. */
export type ResourcesCreateDto = Resources;
