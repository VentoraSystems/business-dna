# components/

Empty on purpose. This sprint is architecture only — no UI. Once
`ExplanationEngine` produces real `ExplanationResult` data, this is where
the explanation-display components (match reasons, strengths, growth
areas, warnings, recommended actions) belong, in the same style as
`src/features/assessment/components/results/`.
