# components/

Empty on purpose. This sprint defines the `BusinessDnaProfile` contract
only — no UI. Once `BusinessDnaProfileRepository` has a real (Prisma-
backed) implementation and `fromBusinessGenome()` is implemented, this is
where any future authoring/browsing UI for the contract would live.
