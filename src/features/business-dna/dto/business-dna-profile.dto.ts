import type { BusinessDnaProfile } from "../types/business-dna-profile";

/** The profile shape as returned to callers — identical to `BusinessDnaProfile` (types/business-dna-profile.ts). */
export type BusinessDnaProfileDto = BusinessDnaProfile;
